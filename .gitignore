# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.venv*/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets — never commit credentials
.secrets/
.env
*.key

# robolabel outputs (annotations, extracted frames, receipts, gold sets)
*.annotations/
annotations.parquet
human_annotations.parquet
raw_receipts/
subgoal_frames/
demo_out/

# strategy-ablation working dir (large, regenerable: receipts, per-cell segments).
# The committed artifacts are STRATEGY_REPORT.md and eval/so101_split.json; the
# machine results JSONs are copied into the report.
eval_out/
eval_out_grip/

# OS
.DS_Store

# supplementary metrics (regenerable via scripts/compute_metrics.py)
eval_metrics/

# inspect viewer data (regenerable via scripts/build_inspect_data.py; embeds the local gold)
inspect_data/
fresh_*/
grades*.json
